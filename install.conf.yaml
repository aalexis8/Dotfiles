- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.tmux.conf: tmux.conf
    ~/.gitconfig:
    ~/.zshrc:

- create:
    - ~/downloads
    - ~/.vim/undo-history

- shell:
#  - [git submodule update --init --recursive, Installing submodules]
  - command: ./test.zsh
    stdout: true
    stderr: true

#  - echo "Message from the shell section: Hello"
#  - sleep 5
#  - ./test.zsh
