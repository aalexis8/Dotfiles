- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#     ~/.vim: vim
     ~/.zshrc: 
     ~/.gitconfig: 

- create:
    - ~/Work
    - ~/.vim/undo-history

      #- shell:
  #   - [git submodule update --init --recursive, Installing submodules]
